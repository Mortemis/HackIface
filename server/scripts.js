const express = require('express');
const router = express.Router();

/*
router.get('/api/scripts/dropcache', function (req, res) {
    var data = req.query;
    const passwd = 'gurrenlagann';
    if (data.pass == passwd) {
        res.send(true);
        //TODO drop cache script running here
    } else {
        res.send(false);
    }
});
*/

module.exports = router;